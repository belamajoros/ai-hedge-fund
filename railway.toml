# Railway-specific deployment configuration

[build]
provider = "docker"
dockerfilePath = "docker/Dockerfile"

[deploy]
startCommand = "cd /app && ./run.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3